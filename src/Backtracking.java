public class Backtracking {
    public static void shortest_path(Point current, Point destination, Solution s){
        if (current.equals(destination)){

        }else{

            for(int i =0; i<current.getPaths().size();i++){
              // s.setTotal_cost(s.getPath().get(i);
            }
        }

    }
}
